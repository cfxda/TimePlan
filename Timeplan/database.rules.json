{
  "rules": {
    ".read": true,
    ".write": false,

    "uploads": {
      ".write": "auth != null"
    },
   "items": {
        ".read": "$uid === auth.uid",
         ".write": "$uid === auth.uid"
    },
    "users": {
      ".write": "auth != null"
    }



  }
}
